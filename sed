#!/bin/bash

cat "$1" > "$2"
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt >> dummyfile.txt
cat dummyfile.txt > 128bytes
